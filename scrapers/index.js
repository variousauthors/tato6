const scrapeFileId = require('./scrapeFileId')
const scrapeFilename = require('./scrapeFilename')
const scrapeFileURL = require('./scrapeFileURL')
const scrapeMD5 = require('./scrapeMD5')

module.exports = {
  scrapeFileId,
  scrapeFilename,
  scrapeFileURL,
  scrapeMD5,
}